# Core dependencies
flet>=0.27.0,<1.0
claude-agent-sdk>=0.1.5
watchdog>=4.0.0
aiofiles>=24.0.0
Pygments>=2.18.0
anyio>=4.0.0

# Optional: SeekDB (Linux embedded mode only - macOS/Windows not supported)
# pyseekdb>=1.0.0

# Development dependencies
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
ruff>=0.5.0
mypy>=1.10.0

# Build dependencies
# pyinstaller>=6.0.0
