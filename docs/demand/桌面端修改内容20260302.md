- [x] 聊天模块的右侧栏工作目录部分，鼠标右键打开菜单，有一个选择选项，选中某个文件或文件夹后右键点击选择后，会在用户聊天的对话框中加入当前文件或文件夹的绝对路径，展示形式是："@文件绝对路径"。以及在用户聊天对话框中输入"@"这个符号后，会打开一个悬浮窗，里面可以选择当前目录下的文件，点击选中之后同样会在对话框中直接加入绝对路径。输入@符号后悬浮窗中的文件显示要动态的显示，也就是说例如输入 @mis 会显示当前工作目录下的所有带有 mis 字符串的所有文件或文件夹，如果继续选中其中一个叫 misaka 的文件夹后，会自动在文件夹后加上 "\\" 符号，并展示misaka文件夹下的一级目录下的文件，之后的输入以此类推。当然若用户直接选择了文件夹或某个文件，或者输入完直接在输入框中单击一下，则直接结束悬浮窗的展示选择。

- [x] 插件模块部分修改，直接修改为 MCP，且MCP设置要支持可编辑。

- [ ] 修复一个问题。在设置模块中选择设置了权限模式，但是在用户在聊天窗口进行对话时，claude agent sdk 并没有遵循这个权限设置，检查一下是否需要传递相关参数。理想的实现效果是，若权限模式选择默认，则当用户进行对话时，模型涉及到增删改查这些操作，都需要对用户进行询问确认之后，再根据用户的选择来往下执行。所以这里聊天窗口中聊天内容的显示这部分，还需要加上若权限判断的UI展示与选择交互功能。同样，若权限模式选择接受编辑，则查询和修改可以自动执行，增加与删除操作需要询问；若权限模式选择跳过所有，则模型的一切操作都不需要询问用户，直接操作就可以。这部分功能没有加上，需要加上这部分的内容。在对用户进行是否执行的询问时可以有三个选项：是、是且之后该操作不在询问、否。对这个功能中的对话交互功能可以单独做个组件模块化嵌入，更灵活更易于维护

- [x] 聊天模块中已经导入的会话列表添加将会话移除列表功能，即可以将某个会话移出客户端列表，但是在本地 session 中还是存在的。

- [x] 设置模块中，去除权限模式下的 Claude 调试日志开关功能。这个调试日志开关直接通过配置文件来控制，不需要做到客户端页面上显示控制。

- [ ] 聊天模块中，添加多会话并行执行支持。也就是说在当前会话的与模型对话未结束的情况下重新开一个会话，对话未结束的会话不会被打断，会持续在后台继续运行直至完成。所以在会话列表中需要添加是否结束会话的小标识。即若当前会话的对话未结束时新开一个会话，那么在未结束会话的列表上会显示一个黄色小圆点闪烁动画，直到此会话在后台运行结束后，黄色小圆点闪烁会变为绿色小圆点闪烁。直到用户点击此会话查看结果，绿色小圆点则消失。

- [ ] 聊天模块，添加项目级别的sessions和会话级别session

- [x] 代码文件结构整理，按照模块来分类，即创建 chat、settings、mcp、skills 文件夹，将每个模块对应的代码文件整理到对应的文件夹中，公共模块整理到 common 文件夹中，涉及到一些设计模式啊、异常啊、handle 等等不用类型的代码也都创建不同的文件夹放入。原则就是不要所有代码文件都平铺在一个文件夹下，在保持大结构的前提下，也还需要有清晰的结构和分类。前后端的代码都根据这个规则来进行分类

- [ ] 聊天模块中，聊天对话框的内容显示，对于 Markdown 和 文字的展示与渲染增强，使其展示的更加美观。

- [x] 左侧菜单栏菜单摆放顺序调整，从上到下分别是：聊天、MCP、技能、设置。

- [ ] 设置模块中，关于 Misaka 软件的在线更新检查与自动安装功能要如何实现，生成一个详细的解决方案说明书。

- [ ] MCP 模块添加 MCP 市场搜索与点击添加功能，设计这个功能

- [ ] 技能模块添加 Skills 市场搜索与点击添加功能，设计这个功能

- [ ] 点击上传附件要打开文件选择器。

- [ ] 打包软件会先闪现弹出n个窗口再关闭，再正常打开exe软件。以及打包后的exe图标不正确，变成默认图标了，不是自定义的小闪电图标。

